runAlias unloadCFGAll
runAlias +freeCursor

## IMPORTANT: do not define NOP_792, a placeholder for NOP to escape loops

## -- basic --
# on press event, keep state across 1 tick
# timing: 1
alias jump_once +jump wait\1 -jump

# on consume click_count
alias drop_single +drop -drop
alias tap_use +use -use

## -- auto loops --
## IMPORTANT: auto loop without timing CAUSES StackOverflow
# SAFETY: jump_once timing 1
alias +auto_jump +silent alias\auto_jump_iter;jump_once;builtinRunAlias\auto_jump_iter -silent builtinRunAlias\auto_jump_iter
alias -auto_jump +silent alias\auto_jump_iter;NOP_792 -silent

alias +auto_tap_use +silent alias\auto_tap_use_iter;tap_use;wait\1;builtinRunAlias\auto_tap_use_iter -silent builtinRunAlias\auto_tap_use_iter
alias -auto_tap_use +silent alias\auto_tap_use_iter;NOP_792 -silent
alias +auto_tower var\lastPitch\pitch setPitch\90 +auto_jump +auto_tap_use
alias -auto_tower -auto_tap_use -auto_jump setPitch\lastPitch
alias +auto_attack_sword +silent alias\auto_attack_sword_iter;+attack;wait\1;-attack;wait\13;builtinRunAlias\auto_attack_sword_iter -silent builtinRunAlias\auto_attack_sword_iter
alias -auto_attack_sword +silent alias\auto_attack_sword_iter;NOP_792 -silent

# `+auto_drop wait\N -auto_drop` drops N+1 items
alias +auto_drop +silent alias\auto_drop_iter;drop_single;wait\1;builtinRunAlias\auto_drop_iter -silent builtinRunAlias\auto_drop_iter
alias -auto_drop +silent alias\auto_drop_iter;NOP_792 -silent

## -- stack split: drop (count_subtracted_1 + 1) from src, move remained-src park, pickup predicted_pickup, move pk dsc, move park src --
# timing: count_subtracted_1 + 40
# warning: unexpected behavior when tick rate higher than normal(20)
# requirements: 1.not standing on edge (making pickup predictable when pitch is 90);
#               2.predicted_pickup: must be the index-smallest empty inventory slot(1-36, 41), NO not-full inventory slots that have same item type as src (mergeable) EXISTS when pickup happens(>= 39 ticks after -auto_drop && near dropped item);
#               3.park: under a screen which has inputable c<N> slot(s);
# CALL: `var\src\<[c]N> var\park\c<N> var\count_subtracted_1\<N> var\predicted_pickup\<N> var\dsc\<[c]N> split_item_stack_from_src_to_dsc`
alias split_item_stack_from_src_to_dsc var\lastPitch\pitch setPitch\90 swapSlot\src\14 wait\1 +auto_drop wait\count_subtracted_1 -auto_drop wait\1 swapSlot\14\park wait\38 swapSlot\park\src swapSlot\predicted_pickup\dsc setPitch\lastPitch

## -- tick rates --
# requirements: server privilege && tick command explicitly allowed by host
alias tr_20 sendCommand\"tick rate 20"
